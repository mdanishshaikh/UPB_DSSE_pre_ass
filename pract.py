import numpy as np

# Create a list of numbers
data = [1, 2, 3, 4, 5]

# Convert to a NumPy array and calculate the mean
array_data = np.array(data)
average = np.mean(array_data)

print(f"The average of my data is: {average}")